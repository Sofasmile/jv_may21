package mate.lesson.ocp.second;

public class NHLSportInfoBuilder implements SportInfoBuilder {

   @Override
   public SportInfo build(String[] data) {
      SportInfo sportInfo = null;
      // код для аналізу NHL
      return sportInfo;
   }
}