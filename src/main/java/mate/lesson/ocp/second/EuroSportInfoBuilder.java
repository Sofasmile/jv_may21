package mate.lesson.ocp.second;

public class EuroSportInfoBuilder implements SportInfoBuilder {

   @Override
   public SportInfo build(String[] data) {
      SportInfo sportInfo = null;
      // код для аналізу euro
      return sportInfo;
   }
}